<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Academic Progression Login</title>
  <link rel="stylesheet" href="/styles.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>
<body class="login-page">

  <header class="login-banner">
    <img src="/icons/bannerlogo.png" alt="University Logo">
  </header>

  <div class="login-wrapper">
    <div class="login-card">

      <div class="login-branding">
        <img src="/icons/bannerlogo.png" alt="Logo" class="branding-logo">
        <div class="branding-text">
          <h1>Academic Progression Portal</h1>
          <p>Welcome to the Web Information System</p>
        </div>
      </div>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert error">
          <%= error %>
        </div>
      <% } %>

      <form method="POST" action="/login" class="login-form">
        <div class="form-group">
          <label for="userField">User ID</label>
          <input
            type="text"
            id="userField"
            name="userField"
            placeholder="Enter your Admin / Student number"
            pattern=".{4,}"
            required
            title="Must be at least 4 characters"
          >
        </div>

        <div class="form-group">
          <label for="password">Password</label>
          <input
            type="password"
            id="password"
            name="password"
            placeholder="Enter your password"
            minlength="6"
            required
          >
        </div>

        <div class="form-group">
          <button type="submit" class="login-button">
            Sign In
          </button>
        </div>
      </form>

      <div class="login-support">
        <i class="fas fa-question-circle"></i>
        <a href="/forgot-username">Forgot Username</a> |
        <a href="/forgot-password">Forgot Password</a>
      </div>

      <div class="login-support">
        <i class="fas fa-headset"></i>
        Need help? Contact support:
        <br>
        <a href="tel:+447567432123">
          <i class="fas fa-phone"></i> +44 7567 432123
        </a> or 
        <a href="mailto:loginsupport@kuniversity.com">
          <i class="fas fa-envelope"></i> loginsupport@kuniversity.com
        </a>
      </div>

      <footer>
        <p>University Portal © <%= currentYear %></p>
      </footer>

    </div>
  </div>

</body>
</html>
