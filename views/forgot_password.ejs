<!DOCTYPE html>
<html>
<head>
  <title>Forgot Password</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <%- include("partials/banner", { req }) %>

  <main class="page-container">
    <h1>Forgot Password</h1>

    <!-- success or fail -  - success shows confirmation that reset email was sent
      / error shows error message -->
    <% if (success) { %>
      <div class="alert success"><%= success %></div>
    <% } else if (error) { %>
      <div class="alert error"><%= error %></div>
    <% } %>

    <!--  Form for submitting the user’s email to request a password reset.
      Posts back to the same `/forgot-password` route. -->
    <form action="/forgot-password" method="POST" class="summary-card">
      <div class="form-group">
        <label for="email">Enter your registered email:</label>
        <input type="email" name="email" id="email" required>
      </div>

       <!-- Buttons - submit to reset password, or cancel and go back to login -->
      <div class="button-group" style="display: flex; gap: 1rem; margin-top: 1rem;">
        <button type="submit" class="button">Reset Password</button>
        <a href="/" class="button-outline">Back to Login</a>
      </div>
    </form>
  </main>

  <!-- Footer displaying current year dynamically -->
  <footer>
    <p>University Portal © <%= new Date().getFullYear() %></p>
  </footer>
</body>
</html>
