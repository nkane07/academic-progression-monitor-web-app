<!DOCTYPE html>
<html>
<head>
  <title>Add New Module</title>
<link rel="stylesheet" href="https://cdn.simplecss.org/simple.min.css">
  <link rel="stylesheet" href="/styles.css">
  
</head>
<body>
  <header>
    <h2>Add New Module</h2>
    <nav>
      <a href="/admin/modules" class="button">Back to Modules</a>
      <a href="/admin" class="button">Dashboard</a>
      

      </a>
      
      <a href="/logout" class="button">Logout</a>
    </nav>
  </header>

  <main class="container">
    <% if (req.query.added) { %>
        <div id="successBanner" class="alert success">
          <i class="fas fa-check-circle"></i> Module added successfully!
        </div>
      <% } %>

      <% if (req.query.duplicate) { %>
        <div class="alert danger">A module with this code already exists.</div>
      <% } %>
      
      
    <form action="/admin/modules/new" method="post" class="form-section">
      <label>Module Name:
        <input type="text" name="module_name" required>
      </label>

      <label>Module Code:
        <input type="text" name="module_code" required>
      </label>

      <label>Credit Value:
        <input type="number" name="credit_value" required>
      </label>

      <label>Pathway Code (e.g. IFSY, BSAS):
        <input type="text" name="degree_programme_code" required>
      </label>

      <label for="semester">Semester:</label>
<select name="semester" id="semester" required>
  <option value="">-- Select Semester --</option>
  <option value="AUT">Autumn</option>
  <option value="SPR">Spring</option>
  <option value="FYR">Full Year</option>
</select>

      </label>

      <button type="submit" class="button">Add Module</button>
    </form>
  </main>

  <footer>
    <p>University Portal © <%= currentYear %></p>
  </footer>
  <script>
    setTimeout(() => {
      const banner = document.getElementById('successBanner');
      if (banner) {
        banner.style.transition = 'opacity 0.5s ease';
        banner.style.opacity = '0';
        setTimeout(() => banner.remove(), 500);
      }
    }, 4000);
  </script>
  
</body>
</html>
