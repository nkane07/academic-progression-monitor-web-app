<!DOCTYPE html>
<html>
<head>
  <title>Forgot Username</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <%- include("partials/banner", { req }) %>

  <main class="page-container">
    <h1>Forgot Username</h1>
    <form action="/forgot-username" method="POST" class="summary-card">
      <div class="form-group">
        <label for="email">Enter your registered email:</label>
        <p style="font-size: 0.9rem; color: #666;">
          Students: use your <em>university contact email</em><br>
          Admins: use your <em>university account email</em>
        </p>
        <input type="email" name="email" required>
        
      </div>
    
    </form>

    <% if (username) { %>
      <div class="alert success">Your username is: <strong><%= username %></strong></div>
    <% } else if (error) { %>
      <div class="alert error"><%= error %></div>
    <% } %>

    <div class="button-group" style="display: flex; gap: 1rem; margin-top: 1rem;">
        <button type="submit" class="button">Recover Username</button>
        <a href="/" class="button-outline">Back to Login</a>
      </div>
      
      
  </main>
</body>
</html>
